bin/
obj/
publish/
dist/
*.user
*.suo
.DS_Store
.vs/
TestResults/

# Package lock files ARE committed for supply-chain reproducibility
# (RestorePackagesWithLockFile + CI --locked-mode)

# Certificates — never commit private keys or PFX files
certs/
*.pfx
*.key
*.pem
!*pub.pem
