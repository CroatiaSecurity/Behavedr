# Platform monitors

| Monitor | OS |
|---------|-----|
| WindowsMonitor | Windows |
| LinuxMonitor | Linux |
| MacOSMonitor | macOS |
| AndroidMonitor | Android |
| IosMonitor | iOS / Mac Catalyst |

Registered at runtime via `PlatformMonitors` / `AgentBootstrap` when `IsSupported` is true.
